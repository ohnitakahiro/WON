<div class="NewRecord">
  <div class="NewRecord＿Box"> 
    <h1>メニュー新規作成</h1>
    <div class="NewRecord＿Box＿Form">
      <div class="NewRecord＿Box">
        <%= form_with(model: @menu_select) do |f| %>
          <div class="field">
            <%= f.label :"種目名を入力" %>
            <%= f.text_field :menu_name %>
          </div>
          <%= f.submit "新しいメニューを保存する", class: "btn btn-success"%>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>